{
  "name": "zhrx-cli",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "init": "pnpm install",
    "test": "lerna run test",
    "publish": "lerna publish"
  },
  "keywords": [
    "zhrx-cli"
  ],
  "author": "durant",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}